Classroom Info
<p>
<%= @classroom.course_name %>
<%= @classroom.semester %>
</p>

We now want to call out the oldest student in the class:

  <!-- must place render inside <ul> because the partial already uses <li> -->
  <%= render "students/student" %>

